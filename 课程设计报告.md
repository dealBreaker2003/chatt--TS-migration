# 1. 课程设计任务书

本项目旨在开发一个基于 Vue 3 的现代 Web 智能聊天应用程序（Chat Bot）。项目提供用户认证、多会话管理、图文混合对话等核心功能，并通过对接服务端与第三方大语言模型，实现智能对话与交互。前端要求采用响应式设计和现代化 UI 交互（如无边框设计、渐变悬停效果等），保证良好的用户体验与操作流畅度。

# 2. 方案设计与功能说明

## 2.1 总体方案设计

本系统采用前后端分离的架构方案。前端使用 Vue 3 (Composition API) 结合 Vite 构建，主要负责 UI 呈现、状态管理（利用 Vue 的响应式 API `ref` 和类实例）以及与后端的 HTTP 通信。后端（服务端地址等已通过环境变量或代理配置）提供 RESTful API 与前端进行交互，进行用户鉴权、会话数据持久化管理以及代理请求大语言模型服务。

## 2.2 各功能模块设计

本系统主要包含以下几个核心模块：

- **用户鉴权模块**：位于 `userLogin.vue`，提供用户登录、注册与退出功能，并在前端利用 LocalStorage 维护用户的 `token` 和基本信息，保持登录状态。
- **会话管理模块**：位于左侧边栏（`conversations.vue`），负责展示历史会话列表，支持新建对话、删除对话以及重命名对话标题，选中时具有深色动态高亮反馈，操作按钮通过 `@click.stop` 处理事件冒泡。
- **对话交互模块**：位于界面主区域（`mainTab.vue`），支持发送文本、图片，实现气泡式的对话流展示（依据 `role` 区分用户和 AI 角色）。包含页面自动滚动到底部、剪贴板图片粘贴识别，以及利用 `IntersectionObserver` 自定义指令 `v-observe` 实现的图片懒加载功能。
- **状态管理模块**：在 `src/states/` 目录下统一定义，按领域划分管理用户状态 (`user.js`)、对话列表 (`conversation.js`) 及当前消息列表 (`message.js`) 等全局响应式共享数据。

# 3. 程序框图（或类图，或算法原理）

## 3.1 核心组件与交互流程图

**组件树层级结构：**
- `App.vue`（应用根节点）
  - `Conversations.vue`（左侧会话列表组件，定宽 180px）
  - `MainTab.vue`（右侧主聊天交互区）
    - `UserLogin.vue`（按需弹出的登录组件）
    - `ImageContainer.vue`（对话流中的图片渲染组件）

**核心交互算法/流程（以消息发送为例）：**
1. 用户在 `MainTab` 输入内容或添加图片，按下 Enter 键触发发送。
2. 前端实例化 `Message` 对象（包含文本与 Base64 图片），推入响应式数组 `messages`，利用 Vue 更新 DOM，并通过 `nextTick` 将滚动条平滑滚动至底部。
3. 携带 `token` 和 `conversationId` 向后端接口 `/api/chat/message` 发起 POST 请求。
4. 接收后端返回数据。如果是新对话，则同步更新对话 ID；同时将 AI 助手的回复封装为新的 `Message` 对象推入视图。

# 5. 程序运行测试与分析

由于本系统主要为具备图形用户界面的前端 Web 应用，程序的函数调用（如 `sendMessageIn`、`createNewConvo`）均与用户操作行为强绑定，不涉及纯粹的独立复杂算法。因此，测试数据和用例设计主要针对**前端组件业务函数**在正常、边界和异常数据输入下的表现，以证明函数执行的健壮性。

## 5.1 测试程序与数据设计

**1. 消息发送功能函数测试 (`sendMessageIn` 函数)**
- **常规测试数据**：设计输入普通的文本（如："你好，请介绍一下你自己"）。
- **预期结果**：输入框清空，消息实体推入 `messages`，界面底部正确渲染用户的绿色气泡。随后调用接口，一段时间后渲染带有模型回复的系统气泡。
- **边界与特殊情况测试**：
  - **空字符边界**：不输入任何文字，直接按下 `Enter` 键。预期结果是前端函数内发生前置校验拦截（`messageContent.value === ''`），不产生任何网络请求和 UI 改变。
  - **超长文本/特殊符号测试**：输入包含几千字符的大段文字或特殊表情符号。用于验证函数封装 JSON `body` 时的字符串转义是否报错，以及渲染 DOM 时长内容是否溢出容器界限（期待自动换行或出现滚动条）。

**2. 核心状态与会话交互功能测试 (`createNewConvo` 与 `handleConvoDelete` 函数)**
- **常规测试数据**：鼠标点击左侧的新建按钮或删除按钮。
- **预期结果**：列表响应式更新新增一条 `newConvoX`（X 自动累加），或对应条目消失；全局状态 `conversationIdRef` 更新；清空右侧主聊天的历史记录。
- **特殊情况测试**：
  - 在只有一个会话记录的情况下点击删除，测试删除唯一会话后，函数是否能妥善处理空数组索引，会不会导致程序抛出 `undefined` 崩溃异常。

**3. 文件图文混合识别函数测试 (`handleFileChange` 及 `handlePaste` 函数)**
- **测试数据**：从剪贴板粘贴文字或通过 `<input type="file">` 上传不支持的文件格式（如 `.txt` 替代图片）。
- **预期结果**：仅将 `file.type.includes('image')` 的文件进行 FileReader 转换，过滤非法格式，避免转 Base64 逻辑崩溃。

## 5.2 运行结果与分析

*(注意：在提交纸质或最终版报告时，请将下方占位符替换为您实际运行系统的操作截图)*

1. **登录鉴权结果**
   通过输入有效账号密码，能够成功获取 Token 并隐藏登录弹窗，右上方正确显式获取的 Username，验证了业务与后台状态联动成功。
   `[此处插入截图：登录成功后的主界面顶部栏展示]`

2. **正常图文对话流程与 UI 分析**
   输入常规问候语，成功获得模型响应。发送和接收气泡颜色正常区分，发送者靠右，接收者靠左。由于实现了动态 Flex 和 `nextTick` 滚动函数，长对话超出 100vh 屏幕高度时，对话框能平滑滚动至最底端。
   `[此处插入截图：包含图文并茂的对话记录界面]`

3. **异常处理机制展示**
   **场景**：模拟后端返回 403 错误（如第三方平台账户余额不足）。
   **分析**：函数运行结果没有使页面卡死，抛出的异常被正确截获，并转换成了友好的用户可见字符串 `"请求失败：Sorry, your account balance is insufficient"` 显示在了系统回复气泡中。充分证明系统函数的异常隔离与错误透传设计达到预期。
   `[此处插入截图：聊天气泡显示红色或文本的“请求失败: 余额不足”提示截图]`

**总结**：
针对各函数业务设计了覆盖了正常使用、边界操作（空白输入）和异常捕获的系统性测试用例。从界面反馈和网络抓包结果看，各项函数均顺利完成预设逻辑，证明了主要代码清单中设计的组件方法逻辑正确，最终完美达成了本次课程设计的任务要求。

# 6. AI 使用情况说明

在本次开发过程中，我们积极引入并合理利用了 AI 大语言模型辅助开发（主要使用了基于主流大语言模型如 ChatGPT/CodeBuddy 等工具）：
- **后端代码辅助**：由于自身主要侧重前端开发，我在了解了一定的后端网络通信知识与接口规范后，由 AI 辅助生成了 Node.js/Express 后端的基础代码模板。
- **UI 样式优化**：在页面核心骨架完成后，AI 在前端 CSS UI 的美化（如无边框设计、渐变悬停效果等细节排版）上提供了样式代码参考和搭建辅助。
- **独立完成环节**：所有前端的核心 JS 逻辑代码、数据框架的设计、以及技术难点的攻克均由本人独立弄明白并手写完成。同时，后端的服务器部署、环境配置及服务上线投入使用等运维工作也均由本人亲力亲为。

# 7. 组内分工情况及贡献度自我评价

本项目由本人与于嘉豪同学组成双人小组共同完成，具体分工及评价如下：
- **本人**：主要负责项目的前端核心逻辑（JS/Vue3 组合式 API）、数据流框架设计及全部的技术选型。此外，负责了后端服务的搭建、调试与最终的服务器上线工作，承担了本系统绝大部分的程序编写任务。
  - **自我评价**：在项目中发挥了核心主导作用，技术落地执行力强，对整体系统架构把控清晰，贡献度较高。
- **于嘉豪**：与我共同完成了早期的系统总体设计规划。主要负责了项目文档与实验报告的编写（除纯技术核心代码部分外），并在 AI 的辅助下完成了系统 UI 基本样式的初步搭建与排版。
  - **评价**：文档撰写详实，工作态度认真，为系统的前期设计和最终汇报材料做出了重要贡献。

# 8. 结论与心得

## 8.1 优点
1. **架构清晰，响应式体验好**：前端采用 Vue 3 结合面向对象的 Class 实体构建响应式数据，界面动态更新流畅，组件拆分（会话列表、主聊天区、鉴权）解耦合理。
2. **错误处理健壮**：对诸如第三方模型 403 报错（余额不足等）实现了优雅的前端透传，没有让页面崩溃或出现“假死”，保证了极佳的用户体验。
3. **技术链路完整**：实现了从前端页面构建到后端服务代理，再到云端服务器上线部署的全栈闭环，是一个完整且可用的现代 Web 应用。

## 8.2 不足
1. **测试覆盖率**：目前主要以手动黑盒 UI 测试和基本逻辑验证为主，缺乏前端自动化测试工具（如 Jest/Vitest）的代码级单元测试覆盖。
2. **后端深度设计**：后端主要是为了配合前端大模型转发而通过 AI 辅助快速搭建的，在数据库高并发读写性能调优及更复杂的鉴权安全方面仍有进一步深挖和优化的空间。

## 8.3 收获与体会
本次课程设计是对我过往所学知识的一次绝佳的综合性“大练兵”。此前我系统学习过《计算机网络》和《数据结构》等核心课程，并利用业余时间自学了现代前端开发（Vue 3/Vite）相关技术。在这次 Chat Bot 项目的实战中，我不仅将计算机网络的 HTTP/SSE 通信原理、跨域调试、数据存储结构等理论知识落地到了具体代码中，巩固了专业基础，更让我从零到一完整体验了项目“设计-开发-部署-上线”的全生命周期。我认为完成得相当不错。

同时，本次开发深刻改变了我的编程思维模式。通过在后端代码生成和 UI 搭建环节中合理引入 AI 工具，我深刻体会到：AI 并不是代替我们思考，而是提升效率的“最佳助手”。这不仅大大加速了项目的推进，也让我对如何精准地运用 Prompt 提问、如何审视与改造 AI 提供的代码有了更深层次的理解，为未来的软件开发工作积累了宝贵的工程经验。

## 4.1 主要程序清单

### 4.1.1 `conversations.vue` (会话管理清单)
- **`choseConvo`**:
  - **参数**: `conversation` (Object) - 选中的会话对象实体。
  - **返回值**: `Promise<void>`。
  - **说明/调用关系**: 由会话列表的点击事件触发。调用后端接口获取对应历史消息并将其装载到全局 `messages` 响应式对象中，更新当前 `conversationIdRef`。
- **`createNewConvo`**:
  - **参数**: 无。
  - **返回值**: `Promise<void>`。
  - **说明/调用关系**: 由“New Convo”按钮触发。在全局 `conversations` 列表末尾压入一个新实例，并通过 DOM 操作触发选中状态。
- **`handleConvoDelete`**:
  - **参数**: `conversation` (Object), `index` (Number) - 目标会话实体及其在列表中的索引。
  - **返回值**: `Promise<void>`。
  - **说明/调用关系**: 触发后端 HTTP DELETE 请求删除记录，并同步清除前端列表视图中对应项。
- **`handleTitleChange`**:
  - **参数**: `e` (Event) - 键盘事件对象。
  - **返回值**: `Promise<void>`。
  - **说明/调用关系**: 按下回车后触发，发起 HTTP PUT 请求同步修改后的新标题到后端数据库。

### 4.1.2 `mainTab.vue` (聊天主业务清单)
- **`sendMessageIn`**:
  - **参数**: `event` (Event) - 键盘输入事件。
  - **返回值**: `Promise<void>`。
  - **说明/调用关系**: 读取输入框的 `messageContent` 和已转换的 `imageBase64s` 数组，打包发送给 `/api/chat/message`。并实例化发送和接受的 `Message` 对象推入视图流。
- **`handleFileChange`**:
  - **参数**: `e` (Event) - 文件选择事件。
  - **返回值**: `void`。
  - **说明/调用关系**: 利用 `FileReader` 读取本地文件转化为 Base64 格式，保存至图片池 `imageBase64s` 供发送调用。

### 4.1.3 `userLogin.vue` (鉴权业务清单)
- **`handleLogin`** / **`handleRegister`**:
  - **参数**: 无（绑定内部 `username` 和 `password` 变量）。
  - **返回值**: `Promise<void>`。
  - **说明/调用关系**: 提交账号密码给服务端，获取 JWT `token` 和用户基础信息并存入本地 `LocalStorage`。

## 4.2 数据结构设计

### 4.2.1 逻辑结构设计

本项目运行主要依赖以下逻辑数据实体，及其核心用途：
1. **User 数据 (用户信息)**：
   - 用途：标识使用系统的客户端身份，用于 API 请求的鉴权、以及关联其私有的会话列表。
2. **Conversation 数据 (会话数据)**：
   - 用途：区分用户不同的聊天主题或上下文环境，提供侧边导航栏列表项信息。
3. **Message 数据 (消息数据)**：
   - 用途：记录每次对话的具体细节（包括文本内容、发送时间、包含的图片及收发角色等），用于在主视窗渲染对话气泡流。

### 4.2.2 物理结构设计

在逻辑设计基础上，抽象出的核心数据表类型结构设计如下：

**表1：User (用户表)**
- `id` (INT, 主键)：用户唯一标识，自增。
- `username` (VARCHAR(50))：登录使用的账号名称。
- `password` (VARCHAR(255))：加密后的哈希密码片段。
- `created_at` (DATETIME)：用户注册时间。

**表2：Conversation (会话表)**
- `id` (INT, 主键)：会话唯一标识，自增。
- `title` (VARCHAR(100))：会话显示标题。
- `user_id` (INT, 外键)：关联 `User` 表的 ID，标识归属。
- `created_at` (DATETIME)：会话创建时间。
- `updated_at` (DATETIME)：会话末次活跃时间。

**表3：Message (消息表)**
- `id` (INT, 主键)：消息的唯一标识。
- `conversation_id` (INT, 外键)：关联 `Conversation` 表的 ID，标识所属会话。
- `role` (VARCHAR(20))：发言角色标识，值为 `user` 或 `assistant`。
- `content` (TEXT)：文本聊天内容。
- `imageUrls` (JSON)：携带图片的 URL 链接数组集合。
- `created_at` (DATETIME)：消息创建产生的时间。

## 4.3 系统出错处理设计

### 4.3.1 出错信息
列出系统可能产生的具体出错信息：
1. **HTTP 401 Unauthorized**: 
   - 报错信息："Token 验证失败或已过期"、"用户名或密码错误"。
2. **HTTP 403 Forbidden**:
   - 报错信息："Sorry, your account balance is insufficient" (第三方模型接口账户余额不足) 或鉴权拦截。
3. **网络与通信异常**:
   - 报错信息："Failed to fetch"、"Network Error"。
4. **格式与解析异常**:
   - 报错信息："Unexpected token in JSON at position" (数据流传输中断时发生)。

### 4.3.2 出错相应的处理
上述出错情况对应的系统处理机制：
1. **针对 HTTP 401 处理**: 前端统一拦截该状态码，立刻清空过期的 LocalStorage 凭证记录，并自动呼出 `userLogin` 登录弹窗，强制要求用户重新认证。
2. **针对 HTTP 403 处理 (余额不足/并发限制)**: 利用 Try-Catch 捕获接口抛出的自定义 Error。捕获后并不阻断界面，而是提取出错误信息内容（例如 "请求失败：模型服务暂时不可用"），作为大模型的响应实体追加显示到聊天气泡中，保证用户知情。
3. **针对网络异常处理**: 停止当前的按钮 Loading 等待状态，保留用户刚刚输入的草稿或所选图片内容，避免丢失，以便用户网络恢复后直接点击重试。
4. **针对数据流解析异常处理**: 对于长文本响应过程中截断的情况，保留已经成功解析出的前半段有效文本内容并安全闭合标签，将损坏的后半段数据块丢弃，防止页面 DOM 渲染崩溃。